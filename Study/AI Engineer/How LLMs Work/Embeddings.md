- A space where the words are expressed as a collections of numbers in a very high dimensional vector.
- Usually pre trained Embedding models have pre trained best starting embedding value for each token they encounter.

[[Tokens]] and Embeddings are two completely different things.